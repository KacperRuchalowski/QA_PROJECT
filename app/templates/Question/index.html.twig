{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_question_list'|trans }}
{% endblock %}


{% block body %}



    <h1>{{ 'title_question_list'|trans }}</h1>

    {% if is_granted ('ROLE_ADMIN') %}
        <a href="{{ url('question_create') }}" title="{{ 'label_create_question'|trans }}">
            {{ 'label_create_question'|trans }}
        </a>
    {% endif %}



    {% if pagination is defined and pagination.items|length %}


        <table class="table table-striped">
            <thead>
            <tr>
                <th>{{ knp_pagination_sortable(pagination, 'label_Created_at:'|trans, 'question.createdAt') }}</th>
                <th>{{ 'label_question_title'|trans }}</th>
                <th>{{ 'label_question_content'|trans }}</th>
                <th>{{ 'label_question_category'|trans }}</th>
                <th>{{ 'Actions'|trans }}</th>
            </tr>
            </thead>
            <tbody>
            {% for question in pagination.items %}
                <tr>
                    <td>{{ question.createdAt|date('Y/m/d') }}</td>
                    <td>{{ question.TitleQuestion }}</td>
                    <td>{{ question.Content }}</td>
                    <td>{{ question.category.name }}</td>

                    <td>
                        <a href="{{ url('question_show', {id: question.id}) }}" title="{{ 'label_question_show'|trans }}">
                            {{ 'label_question_show'|trans }}
                        </a>
                        {% if is_granted ('ROLE_ADMIN') %}
                        <a href="{{ url('question_edit', {id: question.id}) }}" title="{{ 'label_question_edit'|trans }}">
                            {{ 'label_question_edit'|trans }}
                        </a>

                        {% if not question.answers|length %}
                        <a href="{{ url('question_delete', {id: question.id}) }}" title="{{ 'label_question_delete'|trans }}">
                            {{ 'label_question_delete'|trans }}
                        </a>

                        {% endif %}

                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="navigation text-center">
            {{ knp_pagination_render(pagination) }}
        </div>

    {% else %}
        <p>
            {{ 'message_empty_list'|trans }}
        </p>
    {% endif %}
{% endblock %}