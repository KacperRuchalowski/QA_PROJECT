{% extends 'base.html.twig' %}

{% block title %}
    {{ 'Question Details'|trans({'%id%': question.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'Question Details'|trans({'%id%': question.id|default('')}) }}</h1>
    {% if question is defined and question|length %}
        <dl class="dl-horizontal">
            <dt>{{ 'Title'|trans }}</dt>
            <dd>{{ question.TitleQuestion|trans }}</dd>
            <dt>{{ 'Content'|trans }}</dt>
            <dd>{{ question.Content }}</dd>
            <dt>{{ 'Category'|trans }}</dt>
            <dd>{{  question.category.name }}</dd>
        </dl>

        {% for answers in question.answers %}

        {% if answers.isBest == 1 %}

            {{ answers.content }}

            {% endif %}
        {% endfor %}



        {% if question is defined and question|length %}
            <div >
                {% for answers in question.answers %}
                    <div>






                        <div class="card-body">
                            <h5 class="card-title">{{ answers.content }}</h5>
                            <p class="card-title"> {{ answers.nick }}
                                {{ answers.email }}
                            </p>
                            <h1 class="card-title"></h1>

                            {% if is_granted ('ROLE_ADMIN') %}


                            <a href="{{ url('answer_edit', {id: answers.id}) }}" title="{{ 'Edit '|trans }}">
                                {{ 'Edit '|trans }}
                            </a>

                            <a href="{{ url('answer_delete', {id: answers.id}) }}" title="{{ 'Delete '|trans }}">
                                {{ 'Delete '|trans }}
                            </a>

                            <a href="{{ url('answer_best', {id: answers.id}) }}" title="{{ 'Is Best '|trans }}">
                                {{ 'Is Best '|trans }}
                            </a>


                        </div>
                        {% endif %}
                    </div>

                {% endfor %}
            </div>

        {% else %}
            <p>
                {{ 'message_empty_list'|trans }}
            </p>

        {% endif %}

        <p>
            <div>
                {{ form_start(form) }}
                {{ form_widget(form) }}

                <div  class="form-group row float-sm-right">
                    <input type="submit" value="{{ 'save'|trans }}" class="btn btn-light">
                </div>

                {{ form_end(form) }}
            </div>



            <a href="{{ url('question_index') }}" title="{{ 'Back'|trans }}">
                {{ 'Back'|trans }}
            </a>


        </p>

    {% else %}
        <p>
            {{ 'message_item_not_found'|trans }}
        </p>
    {% endif %}
{% endblock %}